<!DOCTYPE html>
<html>
<head>
    <title>Login Form</title>
    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <!-- Custom CSS -->
    <link rel="stylesheet" type="text/css" href="/style.css">
    <style>
        body {
            background: linear-gradient(135deg, #ff0000, #0000ff, #000000);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Arial', sans-serif;
            perspective: 1000px;
        }
        .container {
            max-width: 400px;
            width: 100%;
            background-color: rgba(255, 255, 255, 0.9);
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0px 20px 50px rgba(0, 0, 0, 0.2);
            transform: rotateY(10deg);
            transition: transform 0.5s;
        }
        .container:hover {
            transform: rotateY(0deg);
        }
        .logo {
            text-align: center;
            margin-bottom: 20px;
        }
        .logo img {
            max-width: 150px;
        }
        h1 {
            text-align: center;
            margin-bottom: 30px;
            color: #333;
        }
        .box {
            width: 100%;
            padding: 12px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
            font-size: 16px;
            transition: border-color 0.3s, box-shadow 0.3s;
        }
        .box:focus {
            border-color: #007bff;
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.5);
            outline: none;
        }
        #submitLogin {
            width: 100%;
            background-color: #007bff;
            color: #fff;
            padding: 12px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 18px;
            transition: background-color 0.3s, transform 0.3s;
        }
        #submitLogin:hover {
            background-color: #0056b3;
            transform: translateY(-3px);
        }
        .signup-link {
            text-align: center;
            margin-top: 15px;
        }
        .signup-link a {
            color: #007bff;
            text-decoration: none;
            transition: color 0.3s;
        }
        .signup-link a:hover {
            color: #0056b3;
        }
        @media (max-width: 768px) {
            .container {
                padding: 20px;
                transform: rotateY(0deg);
            }
            .box {
                padding: 10px;
                font-size: 14px;
            }
            #submitLogin {
                padding: 10px;
                font-size: 16px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">
            <img src="https://th.bing.com/th/id/OIP.63ELhv-WCjtADIhGihraBQHaHa?rs=1&pid=ImgDetMain" alt="Brand Logo">
        </div>
        <form action="/login" method="post">
            <h1>Login Form</h1>
            <input class="box" type="text" name="username" id="username" placeholder="Username" required /><br>
            <input class="box" type="password" name="password" id="password" placeholder="Password" required /><br>
            <br>
            <input type="submit" id="submitLogin" name="submitLogin" value="Login" /><br>
            <div class="signup-link">
                <p class="fw-light mt-2">Don't have an account? <a href="/signup">Sign up</a></p>
            </div>
        </form>
    </div>
</body>
</html>
